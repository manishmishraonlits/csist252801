#include <stdio.h>

int main(void)
{
    for (int i = 1; i <= 5; i++)
    {
        printf("%d. The only way to learn a new programming language is by writing programs in it.\n", i);
    }
    return 0;
}